# Render.com deployment configuration for Vite React app
services:
  - type: web
    name: vite-portfolio
    env: node
    buildCommand: "npm run build"
    startCommand: "npm start"
    staticPublishPath: dist
